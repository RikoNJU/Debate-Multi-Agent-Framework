# 未来交互层预留

当前尚未实现前端。若后续确定采用交互式产品形态，可在此实现以下能力：

- 论文与原 Step 1-3 结果输入；
- 三个 Specialist 的独立意见；
- Debate 问题、回应和外部证据；
- Global Review、Step 4/5 兼容结果和最终评分。

后端默认地址为 `http://localhost:8020`，API 前缀为 `/api/debate`。
